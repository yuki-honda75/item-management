spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/item-management
    username: postgres
    password: postgres
server:
  servlet:
    context-path: /item-management
